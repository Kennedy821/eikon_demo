# EIKON backend base URL (same API the Streamlit app uses)
NEXT_PUBLIC_EIKON_API_BASE_URL=https://slugai.eikon.ngrok.app

# Optional: if CORS blocks direct browser calls, route through Next.js API
# routes instead and set this to a server-only value.
# EIKON_API_BASE_URL=https://slugai.eikon.ngrok.app
